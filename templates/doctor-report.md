# Doctor Report

Use this template when checking whether a checkout is ready for an agent to use as an operating harness.

Schema fields: `schema_version`, `checked_at`, `repo_root`, `python`, `git`, `required_entrypoints`, `public_copy`, `validator`, `readiness`, `warnings`, `blockers`, `next_actions`.

## Environment

- `schema_version`:
- `checked_at`:
- `repo_root`:
- `python`:
- `git`:

## Harness Readiness

- `required_entrypoints`:
- `public_copy`:
- `validator`:
- `readiness`:

## Follow-Up

- `warnings`:
- `blockers`:
- `next_actions`:
